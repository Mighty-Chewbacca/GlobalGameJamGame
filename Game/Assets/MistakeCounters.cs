using UnityEngine;
using System.Collections;

public class MistakeCounters : MonoBehaviour {

    byte BedroomCounter1, BedroomCounter2, StairsCounter, CorridorCounter1,
        CorridorCounter2, KitchenCounter1, KitchenCounter2, FrontdoorCounter = 0;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	    // if you fail a task, increment the respective counter
	}
}
